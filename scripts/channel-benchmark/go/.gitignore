# Compiled binary (local artifact)
/channel-benchmark

# Output artifacts
/benchmark-results/

# Go's default test cache / coverage
*.out
*.test

# Local config (contains tokens)
*.local.yaml
benchmark-ch*.yaml
