"""Image-generation model-substitution detection for Fy-api channels.

Two modes:
  5A — Vendor direct comparison (requires vendor_url + vendor_key)
  5B — No-key substitution detection (fingerprints + cross-channel + capability)
"""
__version__ = "0.1.0"
